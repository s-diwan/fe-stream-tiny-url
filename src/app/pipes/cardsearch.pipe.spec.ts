import { CardsearchPipe } from './cardsearch.pipe';

describe('CardsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CardsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
