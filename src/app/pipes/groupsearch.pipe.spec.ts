import { GroupsearchPipe } from './groupsearch.pipe';

describe('GroupsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
